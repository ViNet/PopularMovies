<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:key="first_preferencescreen">

    <ListPreference
        android:key="@string/pref_order_key"
        android:title="@string/pref_order_title"
        android:summary="@string/pref_order_summary"
        android:dialogTitle="@string/pref_order_dialog_title"
        android:entries="@array/pref_order_entries"
        android:entryValues="@array/pref_order_values"
        android:defaultValue="@string/param_sort_by_popularity_desc" />

</PreferenceScreen> 